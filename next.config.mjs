/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "apod.nasa.gov",
      "www.youtube.com",
      "www.vimeo.com",
      "apod.nasa.gov",
    ],
  },
};

export default nextConfig;
